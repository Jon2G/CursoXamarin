<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:demo="clr-namespace:Demo;assembly=Demo"
             x:Class="Demo.MainPage"
             x:DataType="demo:MainPage">

    <StackLayout Padding="20" BackgroundColor="WhiteSmoke">
        <Stepper />

        <Stepper Maximum="100" Minimum="0"  />

        <Stepper Maximum="100" Minimum="50"   />

        <Stepper Minimum="0" Maximum="1" Value="0.5"  BackgroundColor="AliceBlue"/>

        <Stepper Value="{Binding Valor}" />

        <StackLayout Orientation="Horizontal">
            <Label Text="Valor del stepper:"/>
            <Label Text="{Binding Valor}"/>
        </StackLayout>


    </StackLayout>

</ContentPage>
